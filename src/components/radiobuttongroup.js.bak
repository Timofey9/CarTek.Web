import React, { Fragment } from 'react';

const StateRadioButtonGroup = ({ type, isActive, id }) => {
    console.log(id);
    return
        <div className="row">
            <div className="col-md-2">
                <h3>{type}</h3>
            </div>

            <div className="col-md-5">
                <div className="form-check">
                    <input className="form-check-input" type="radio" name="flexRadioDefault" id={id + 1} checked={isActive === true} />
                    <label className="form-check-label" for={id + 1}>
                        Исправен
                    </label>
                </div>
            </div>
            <div className="col-md-5">
                <div className="form-check">
                    <input className="form-check-input" type="radio" name="flexRadioDefault" id={id + 2} checked={isActive === false} />
                    <label className="form-check-label" for={id + 2}>
                        Не исправен
                    </label>
                </div>
            </div>
        </div>;
};

export default StateRadioButtonGroup;